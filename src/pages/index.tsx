import { Button } from "@nextui-org/button";
import { FC } from "react";

const Home: FC = () => {
    return (
        <main>
            <Button>Press me</Button>
        </main>
    );
};

export default Home;
